There is only one constructor in Alex's code, so there is only one
default constructor. In the main program, only one instance of the
class Average existed, so an explicit copy constructor is not
necessary.

